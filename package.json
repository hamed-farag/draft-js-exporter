{
  "name": "draft-js-exporter",
  "version": "0.0.1",
  "description": "A module to export and format the content from the React Draft.js framework.",
  "main": "main.js",
  "scripts": {
    "build": "babel src -d lib",
    "test": "npm run build && node lib/test.js"
  },
  "keywords": [
    "draftjs"
  ],
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.6.5",
  },
  "babel": {
    "presets": ["es2015"]
  }
}
